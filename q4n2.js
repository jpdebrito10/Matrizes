function n14(){
  background(q4n2)
  if(mouseX>=0 && mouseY>=3 && mouseX<=77 && mouseY<=40){
    noFill()
    stroke(0)
    rect(0,3,70,22)
    if(mouseIsPressed){
      tela=0
} //d
  }if(mouseX>=43 && mouseY>=401 && mouseX<=62 && mouseY<=420){
    noFill()
    stroke(0)
    rect(43,401,70,22)
    if(mouseIsPressed){
      tela=18
}
  } if(mouseX>=85 && mouseY>=295 && mouseX<=108 && mouseY<=320){
    noFill()
    stroke(0)
    rect(85,295,70,22)
    if(mouseIsPressed){
      tela=23
}
  } if(mouseX>=85 && mouseY>=323 && mouseX<=102 && mouseY<=336){
    noFill()
    stroke(0)
    rect(85,323,70,22)
    if(mouseIsPressed){
      tela=23
}
  } if(mouseX>=85 && mouseY>=365 && mouseX<=104 && mouseY<=376){
    noFill()
    stroke(0)
    rect(85,365,70,22)
    if(mouseIsPressed){
      tela=23
}
  }
}